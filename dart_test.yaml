tags:
  version-verify:
    skip: "Should only be run during pull request. Verifies if version file is updated."
  e2e:
    skip: "Requires network access. Run explicitly with: dart test --run-skipped -t e2e"
  sandbox:
    skip: "Boots a guest from example/assets. Run explicitly with: dart test --run-skipped -t sandbox"
