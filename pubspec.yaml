name: dart_emu
description: A RISC-V system emulator ported from TinyEMU
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  args: ^2.6.0
  mason_logger: ^0.3.1
  yaml: ^3.1.3

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.0
  very_good_analysis: ^10.1.0

executables:
  dart_emu:
